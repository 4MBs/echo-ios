# Echo iOS

Read [AGENTS.md](AGENTS.md) before changing anything here. The rule that gets
broken most often, so it is repeated here:

**Every change goes through a pull request.** Never commit or push to `main`,
and never leave a pushed branch without opening its PR — that is how `main` and
a branch end up with two sets of fixes that have to be reconciled by hand.
