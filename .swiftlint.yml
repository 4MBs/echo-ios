included:
  - Sources
  - Tests

excluded:
  - LocalPackages

line_length:
  warning: 120
  error: 160

identifier_name:
  min_length: 2
  excluded: [id, ws, t0, t1, a, b]

type_body_length:
  warning: 400

function_body_length:
  warning: 80

disabled_rules:
  - todo
  - trailing_comma
