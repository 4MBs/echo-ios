After committing changes, immediately push the commit to GitHub so the user can see it.

Use the iOS Simulator only when the user explicitly asks you to use it. Do not start, boot, or run a simulator proactively, including through local tools or GitHub Actions.
