# generated by xcodegen — never commit the project file
*.xcodeproj
*.xcworkspace

# build products
build/
DerivedData/
.spm-packages/
*.ipa
*.xcarchive
*.xcresult

# SPM
.build/
Package.resolved

# signing material must never enter the repo
*.p12
*.mobileprovision
*.cer
*.certSigningRequest
AuthKey_*.p8
ExportOptions.plist

# local config
Config.plist
.DS_Store
